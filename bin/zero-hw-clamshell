#!/bin/bash

# zero:summary=Returns true when clamshell mode is active
# zero:hidden=true

# Clamshell = lid closed while driving one or more external monitors.
zero-hw-laptop-closed && zero-hw-external-monitors
