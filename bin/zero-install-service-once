#!/bin/bash

# zero:summary=Install the ONCE service, enable its background service, and launch the TUI.
# zero:requires-sudo=true

echo "Installing ONCE..."
zero-pkg-add once-bin

echo "Enabling ONCE background service..."
sudo systemctl enable --now once-background.service

echo -e "\nLaunching ONCE..."
sudo once
