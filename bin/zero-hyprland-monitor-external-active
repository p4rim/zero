#!/bin/bash

# zero:summary=Returns true when Hyprland has an active external monitor
# zero:hidden=true

hyprctl monitors all -j | jq -e '.[] | select(.name | test("^(eDP|LVDS|DSI)-") | not) | select(.disabled == false)' >/dev/null 2>&1
