#!/bin/bash

# zero:summary=Remove Steam and all of its game libraries, configs, and caches.
# zero:requires-sudo=true

set -e

zero-pkg-drop steam

rm -rf \
  "$HOME/.steam" \
  "$HOME/.local/share/Steam" \
  "$HOME/.config/steam" \
  "$HOME/.cache/steam"

echo ""
echo "Steam and its data have been removed."
