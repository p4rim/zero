#!/bin/bash

# zero:summary=Dismiss a notification by summary substring. Used by the first-run notifications to dismiss them after clicking for action.
# zero:args=<summary>

if (($# == 0)); then
  echo "Usage: zero-notification-dismiss <summary>"
  exit 1
fi

zero-shell -q notifications dismiss "$1"
