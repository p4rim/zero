#!/bin/bash

# zero:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.local/state/zero/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  ZERO_THEME_SKIP_BACKGROUND=1 zero-theme-set "$(cat $THEME_NAME_PATH)"
fi
