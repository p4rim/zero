#!/bin/bash

# zero:summary=Remove Tailscale and its bar plugin.
# zero:requires-sudo=true

tailscale down 2>/dev/null || true
systemctl --user disable --now zero-tailscale-receive.service 2>/dev/null || true
sudo systemctl disable --now tailscaled.service 2>/dev/null || true
zero-plugin-disable zero.tailscale
zero-webapp-remove "Tailscale" 2>/dev/null || true
zero-pkg-drop tailscale

echo ""
echo "Tailscale has been removed."
