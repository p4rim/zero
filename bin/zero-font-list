#!/bin/bash

# zero:summary=List available monospace fonts
# zero:examples=zero font list | zero font set "CaskaydiaMono Nerd Font"

fc-list :spacing=100 -f "%{family[0]}\n" | grep -v -i -E 'emoji|signwriting|zero' | sort -u
